PORT=4000
USE_MOCK_ML=true
# Python executable used to spawn ml/inference/inference.py. Defaults to
# "python" on Windows and "python3" elsewhere.
PYTHON_BIN=python3
# Milliseconds before a hung Python inference process is killed.
ML_TIMEOUT_MS=10000
